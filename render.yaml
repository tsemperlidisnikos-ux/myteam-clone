# PostgreSQL only — API runs on Vercel (backend-sigma-six-35.vercel.app)
# Deploy: https://render.com/deploy?repo=https://github.com/tsemperlidisnikos-ux/myteam-clone
# After deploy: copy External Database URL → finish-deploy.ps1

databases:
  - name: myteam-db
    plan: free
    databaseName: myteam
    user: myteam
